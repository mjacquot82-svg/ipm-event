# Grounds staging approval candidate

Reconciled onto canonical staging `23d9e98df9efae30493bf387ae02fad0bf8ba75a`, from prototype `aca902a4aec255318d8bec9fa3f1dc4ab2f63b71` (Build 368726). No prototype history is merged.

General shows the three approved traffic routes and Flow of traffic. Parking shows the existing 15 arrival POIs, with no routes or Flow caption. Both views show the unchanged Durham Road barricade wording, style and placement. The same artwork and camera serve both views. Printed green P symbols remain inside the unmodified JPEG.

Parking anchors remain approximate approval data: numbered entries are MEDIUM confidence; Accessible Parking is HIGH confidence. No #6 or 911 numbers are added. Source PDFs remain read-only; coordinates and labels are identical to the approved prototype.

Map-only prerequisites include the approved desktop workspace, phone heading crop and artwork loading/preload. Calendar sorting alone is reconciled for the requested Sunday-first Schedule gate; staging onboarding, media, stars and all other features remain intact. Staging backend routing, vendor catalog, updater, service-worker logic and WonderPush are unchanged.

The broad 602-test audit has 599 passes and three pre-existing admin/proxy assertions failing identically on the staging base. These expect older admin/proxy behavior, including the obsolete production vendor proxy. Their source and implementation are unchanged. Separate focused staging release gates cover current static vendor routing and the protected features.
