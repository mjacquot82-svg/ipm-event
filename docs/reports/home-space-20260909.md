# Focused Home space correction — 9 September 2026

1. **Elements removed/condensed.** Build 362198 already omitted “Use IPM now”, browser-first explanatory text, installation banners and persistent notification status cards from Home. This patch removes only the redundant “Stay up to date” heading from the remaining optional invitation, places Enable notifications and Not now in one action row, removes the message's extra top margin, and reduces the invitation's vertical padding from 8px to 4px. The explanatory message and both actions remain.
2. **Final hierarchy.** Existing Hero → Countdown → My Next Event when applicable → existing Quick Actions → normal Home content. The optional invitation stays below the complete Quick Actions group. No primary feature was moved or redesigned. Existing conditional announcement/cached-data elements remain unchanged under the exclusions.
3. **Notification prompt.** Unboxed, compact text: “Get important IPM announcements and updates.” followed by “Enable notifications” and “Not now”. Existing eligibility, dismissal storage, cooldown, enrollment handlers and success suppression are unchanged. Buttons retain 44px minimum touch targets. Enabled attendees see no Home prompt; a successful simulated enrollment removes it. Dismissal survives reload. Denied attendees see no Home diagnostics; recovery remains in About → App help. Technical reconciliation terms remain absent from Home.
4. **Installed PWA.** No installation/setup/status wall. Enabled and denied states show no invitation. Eligible disabled users get only the compact invitation below Quick Actions. About → App help is untouched.
5. **Normal browser.** The same compact, optional behavior applies. No installation requirement, automatic permission request or new nagging behavior was added.
6. **Mobile space.** Measured CSS viewports and first primary-content heading:

   | Viewport | My Next Event / Quick Actions begins | Prompt before → after |
   | --- | --- | --- |
   | 320 × 568 | y=455px; visible above bottom navigation | 121px → **92px** |
   | 360 × 640 | y=492px; visible above bottom navigation | 121px → **72px** |
   | 1440 × 900 | y=803px; visible above bottom navigation | 101px → **72px** |

   These are prompt component heights, excluding the unchanged surrounding section margins. The 320px prompt is 24% shorter; the 360px prompt is 40% shorter. Hero/countdown/primary-content positions are unchanged. The browser test covers My Next Event present and absent, installed-mode and ordinary-browser paths. Installed mode is simulated using browser platform/display-mode adapters; no new physical-device certification is claimed.
7. **Files changed.** One application file: `frontend/src/components/NotificationOptIn.tsx`, limited to its Home return markup and three Home styles. Production tests: `frontend/tests/home-space.browser.mjs` (new measured 36-case matrix), `frontend/tests/home-presentation.browser.mjs`, and `frontend/tests/onboarding-runtime.test.mjs` (visible-copy expectation). Staging has the same application patch and browser tests; its existing runtime test needed no copy change. Reports/test evidence are separate. Source comparisons prove all eligibility/enrollment/dismissal logic, non-Home help JSX and other styles are byte-identical to their deployed baselines.
8. **Tests.** Test-first baseline failed the new ≤96px invitation-height assertion at 121px. Production: **263/263** unit tests; TypeScript; build; lint with zero errors and 46 existing warnings. Staging: **78/78** focused notification/onboarding/itinerary/Home tests; TypeScript; build; lint with zero errors and 60 existing warnings. The 36-case browser matrix crosses 320/360/1440 widths × installed/browser × enabled/disabled/denied × My Next Event present/absent. It checks first-screen space, no overflow/setup/technical copy, prompt position/height, dismissal persistence, no automatic permission/enrollment calls, and disappearance after explicit enrollment through inert adapters. Staging and production preview matrices passed. Existing itinerary browser checks verify add/remove confirmations, contextual suggestion dismissal/cooldown and granted-permission suppression. An initial local staging fixture used native worker readiness despite mocked registration; the fixture now supplies inert readiness too. This changed tests only, never application or worker code. Final live verification is recorded below.
9. **Staging deployment.** `6aa15e482ac3deb266aec6a3`, published 13:29:07 UTC; application source `e96f2763ecd468351d7a922223618c52ea79a030`; build **362242**. All 36 live staging cases passed. Remote staging was fast-forwarded to `bbabc4b4`, which additionally records the test-only fixture correction. Independent staging code remains intact.
10. **Production deployment.** `6aa15e88b199e77b4e5fccf7`, published 13:31:22 UTC; application source `1999c69e89b9fdcb77f0cc6ad81f26aa43eb54ae`. The production branch starts from the actual deployed production release, not staging. No staging merge, backend deployment or main push occurred.
11. **Final production build.** **362242**, replacing **362198**. The exact draft package was published after staging and preview gates passed.
12. **Schedule.** Read-only before/after comparison: **218 production events**, identical records. No schedule changes or database writes.
13. **Vendors/map.** **127 vendor records**, identical before/after. Existing map asset hashes and every retained asset are unchanged; no map, vendor, booth/location or interactive Tented City source change.
14. **Reconciliation.** Live health remains **100% repair cohort**, repair/observation enabled, unchanged operating mode and backend commit. No reconciliation, eligibility, provider record or Admin Analytics changes. “100%” describes eligibility, not a claim that every device is VERIFIED.
15. **what3words.** Untouched: source, configuration, backend and existing Emergency action retained.
16. **Service worker.** Source and update behavior untouched. Generated cache version/precache bundle references change normally with the frontend build; normalizing those generated declarations gives identical worker logic on both staging and production. No lifecycle correction or architecture change was made.
17. **Notifications sent.** **Zero.** Browser tests used inert subscription/permission adapters and blocked real provider/backend writes. No announcements, Notify Everyone, enrollment, unsubscribe or provider-record operation was performed against real attendees.

Evidence: `release-artifacts/home-space-20260909/` contains the failing baseline height gate, source invariants/whitelist, build/test logs, deployment manifests, before/after verification, and mobile screenshots. The only application difference is the compact Home invitation.


**Final result: LIVE AND VERIFIED.** Production build **362242** passes all **36 live Home-space cases** and all three itinerary/suggestion browser checks. Mobile screenshots were reviewed, including the final 92px invitation at 320px. Published HTML, JavaScript, generated worker and map asset bytes match the tested package. Before/after data checks confirm 218 identical events, 127 identical vendors and unchanged 100% reconciliation configuration. The approved feature designs, About help, notification logic, what3words and service-worker behavior are unchanged. No notification sent. Work stopped after verification.
