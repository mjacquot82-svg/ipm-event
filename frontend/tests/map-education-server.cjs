// Local verification server only: stub the remote provider while exercising the real generated offline shell.
const http=require('node:http'),fs=require('node:fs'),path=require('node:path');
const root=process.argv[2]||path.resolve(__dirname,'../dist');http.createServer((req,res)=>{let name=new URL(req.url,'http://localhost').pathname;if(name==='/api/vendors')name='/api/vendors.json';if(name==='/test-provider.js'){res.setHeader('Content-Type','text/javascript');return res.end('self.WonderPush=[];');}let file=path.join(root,name);if(!fs.existsSync(file)||fs.statSync(file).isDirectory())file=path.join(root,'index.html');res.setHeader('Content-Type',({'.js':'text/javascript','.json':'application/json','.html':'text/html','.svg':'image/svg+xml','.png':'image/png','.jpg':'image/jpeg','.ttf':'font/ttf'}[path.extname(file)]||'application/octet-stream'));let data=fs.readFileSync(file);if(name==='/webpushr-sw.js')data=data.toString().replace('https://cdn.by.wonderpush.com/sdk/1.1/wonderpush-loader.min.js','/test-provider.js');res.end(data);}).listen(8870,'127.0.0.1');
