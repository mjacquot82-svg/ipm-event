export function isScheduleRecord(record: unknown): boolean;
export function isVendorRecord(record: unknown): boolean;
export function shouldAcceptReplacement(
  previousCount: number,
  nextCount: number,
  response: unknown,
): boolean;
